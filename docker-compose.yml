version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15
    container_name: plazoria-postgres
    environment:
      POSTGRES_DB: plazoria
      POSTGRES_USER: plazoria_user
      POSTGRES_PASSWORD: plazoria_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - plazoria-network

  # Redis for caching and sessions
  redis:
    image: redis:7-alpine
    container_name: plazoria-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - plazoria-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: plazoria-backend
    environment:
      - DATABASE_URL=postgresql://plazoria_user:plazoria_password@postgres:5432/plazoria
      - REDIS_URL=redis://redis:6379
      - SECRET_KEY=your-secret-key-here-change-in-production
      - DEBUG=true
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
      - /app/venv
    networks:
      - plazoria-network
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: plazoria-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - plazoria-network
    command: npm run start

  # Celery worker for background tasks
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: plazoria-celery-worker
    environment:
      - DATABASE_URL=postgresql://plazoria_user:plazoria_password@postgres:5432/plazoria
      - REDIS_URL=redis://redis:6379
      - SECRET_KEY=your-secret-key-here-change-in-production
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
    networks:
      - plazoria-network
    command: celery -A app.celery:celery_app worker --loglevel=info

  # Celery beat for scheduled tasks
  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: plazoria-celery-beat
    environment:
      - DATABASE_URL=postgresql://plazoria_user:plazoria_password@postgres:5432/plazoria
      - REDIS_URL=redis://redis:6379
      - SECRET_KEY=your-secret-key-here-change-in-production
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
    networks:
      - plazoria-network
    command: celery -A app.celery:celery_app beat --loglevel=info

volumes:
  postgres_data:
  redis_data:

networks:
  plazoria-network:
    driver: bridge 